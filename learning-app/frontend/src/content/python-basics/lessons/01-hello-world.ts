export const content = `
# はじめてのPython

プログラミングの第一歩は、画面にテキストを表示することです。Pythonでは **print()** 関数を使います。

## print() 関数

\`print()\` 関数に文字列を渡すと、その文字列が画面に表示されます。

\`\`\`python
print("Hello, World!")
\`\`\`

上のコードを実行すると、画面に \`Hello, World!\` と表示されます。

## 文字列とは

文字列（もじれつ）とは、テキストデータのことです。Pythonでは、ダブルクォーテーション (\`""\`) またはシングルクォーテーション (\`''\`) で囲んで表現します。

\`\`\`python
print("こんにちは")  # ダブルクォーテーション
print('こんにちは')  # シングルクォーテーション（どちらでもOK）
\`\`\`

## f文字列（フォーマット文字列）

変数の値を文字列に埋め込みたいときは、**f文字列** を使います。文字列の前に \`f\` をつけ、変数を \`{}\` で囲みます。

\`\`\`python
name = "太郎"
print(f"こんにちは、{name}さん!")
# 出力: こんにちは、太郎さん!
\`\`\`

## 複数の値を出力

\`print()\` 関数にはカンマ区切りで複数の値を渡せます。

\`\`\`python
print("りんご", "バナナ", "みかん")
# 出力: りんご バナナ みかん
\`\`\`

それでは、演習問題に挑戦してみましょう！
`;
