export const content = `
# 変数とデータ型

## 変数とは

変数は、データを保存するための「名前付きの箱」です。Pythonでは \`=\` を使って値を代入します。

\`\`\`python
x = 10        # 整数を代入
name = "太郎"  # 文字列を代入
pi = 3.14     # 小数を代入
\`\`\`

## 基本的なデータ型

Pythonの主なデータ型は以下の4つです：

- **int** (整数): \`42\`, \`-10\`, \`0\`
- **float** (浮動小数点数): \`3.14\`, \`-0.5\`, \`1.0\`
- **str** (文字列): \`"hello"\`, \`'Python'\`
- **bool** (真偽値): \`True\`, \`False\`

## 型の確認

\`type()\` 関数を使うと、変数のデータ型を確認できます。

\`\`\`python
x = 42
print(type(x))   # <class 'int'>

y = 3.14
print(type(y))   # <class 'float'>
\`\`\`

## 型の変換

データ型を変換する（キャスト）こともできます。

\`\`\`python
# 文字列を整数に変換
num_str = "123"
num = int(num_str)
print(num + 1)  # 124

# 整数を文字列に変換
x = 42
s = str(x)
print("答えは" + s)  # 答えは42
\`\`\`

## 演算子

数値に対しては算術演算子が使えます。

\`\`\`python
a = 10
b = 3

print(a + b)   # 13  (足し算)
print(a - b)   # 7   (引き算)
print(a * b)   # 30  (掛け算)
print(a / b)   # 3.333...  (割り算)
print(a // b)  # 3   (整数除算)
print(a % b)   # 1   (余り)
print(a ** b)  # 1000 (べき乗)
\`\`\`

それでは、演習問題に挑戦しましょう！
`;
