export const content = `
# 深層学習入門

## 深層学習とは

深層学習（ディープラーニング）は、機械学習の一分野であり、多層のニューラルネットワークを用いてデータから複雑なパターンを学習する手法です。

## なぜ深層学習が重要なのか

- **画像認識**: 写真の中の物体を認識
- **自然言語処理**: テキストの翻訳や要約
- **音声認識**: 音声をテキストに変換
- **ゲームAI**: 囲碁や将棋で人間を超える性能

## PyTorchとは

PyTorchはFacebook（Meta）が開発したオープンソースの深層学習フレームワークです。Pythonとの親和性が高く、研究から本番環境まで幅広く使われています。

\`\`\`python
import torch

# PyTorchのバージョン確認
print(f"PyTorch version: {torch.__version__}")

# GPUが利用可能か確認
print(f"CUDA available: {torch.cuda.is_available()}")
\`\`\`

## 深層学習の基本概念

### 1. ニューロン（パーセプトロン）

生物の神経細胞を模倣した計算単位です。入力に重みを掛けて足し合わせ、活性化関数を通して出力します。

\`\`\`
出力 = f(w1*x1 + w2*x2 + ... + b)
\`\`\`

### 2. 層（レイヤー）

ニューロンの集まりです。深層学習では複数の層を重ねることで複雑な表現を学習します。

- **入力層**: データを受け取る層
- **隠れ層**: 中間的な特徴を学習する層
- **出力層**: 最終的な予測を出力する層

### 3. 活性化関数

非線形性を導入する関数です。代表的なものには以下があります：

- **ReLU**: \`max(0, x)\` — 最もよく使われる
- **Sigmoid**: \`1 / (1 + exp(-x))\` — 0〜1の出力
- **Tanh**: \`(exp(x) - exp(-x)) / (exp(x) + exp(-x))\` — -1〜1の出力

### 4. 損失関数

モデルの予測と正解の「ずれ」を数値化する関数です。

- **MSE（平均二乗誤差）**: 回帰問題に使用
- **CrossEntropy（交差エントロピー）**: 分類問題に使用

### 5. 最適化

損失関数を最小化するように、モデルのパラメータ（重みとバイアス）を更新するプロセスです。

## PyTorchの基本操作

\`\`\`python
import torch

# テンソルの作成
x = torch.tensor([1.0, 2.0, 3.0])
print(f"テンソル: {x}")
print(f"形状: {x.shape}")
print(f"データ型: {x.dtype}")

# 基本的な演算
y = torch.tensor([4.0, 5.0, 6.0])
print(f"足し算: {x + y}")
print(f"掛け算: {x * y}")
print(f"内積: {torch.dot(x, y)}")
\`\`\`

## まとめ

深層学習は多層のニューラルネットワークを活用し、データから複雑なパターンを自動的に学習する強力な手法です。PyTorchを使えば、Pythonの直感的な文法で深層学習モデルを構築できます。次のレッスンでは、PyTorchの基本データ構造であるテンソルについて詳しく学びます。
`;
